[project]
name = "atmosphere"
version = "0.1.0"
description = "AI enabled weather app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.27.0",
    "python-dotenv>=1.2.2",
]
